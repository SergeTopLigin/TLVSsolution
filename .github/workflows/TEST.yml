name: TEST

env:
  CRON_SCHEDULE: '*/5 * * * *'
  
on:
  workflow_dispatch:
  schedule:
    - cron: ${{ env.CRON_SCHEDULE }}

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - run: echo "hello"
